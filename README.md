I've started to test answers with assert only starting day 2. Before that, you
can find correct solution and `WA` file with some hints to wrong answers.

Main idea is to use STD lib only, with no external dependencies and code
everything from scratch.
